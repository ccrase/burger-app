<li class="list-group-item">
	{{burger_name}}
    {{#if devoured}}
    {{else}}
    <button type="button" class="btn-sm btn-danger float-right change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
        Devour
	</button>
    {{/if}}
</li>